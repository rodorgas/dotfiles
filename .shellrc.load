# include what you want

export PATH=$PATH:$HOME/bin

# add this to the .bashrc or .zshrc (done automatically by dfm):
# . $HOME/.shellrc.load

####
alias profile-github='git config user.name "Rodrigo Orem";git config user.email orem.rodrigo@gmail.com'
alias profile-gitlab='git config user.name "Rodrigo Orem";git config user.email rodrigo@prazocerto.com.br'

# set window title to ssh host
ssh() {
    tmux rename-window "$*"
    command ssh "$@"
    exit
}
